const showMessage = function (message) {
    console.log(message);
}
 showMessage("Hello, World!");  // wywołanie funkcji showMessage z argumentem "Hello, World!"

const showMeTextMyArrowFunction = (text) => {
    console.log(text);
}
 showMeTextMyArrowFunction("hej"); // wywołanie funkcji showMeTextMyArrowFunction z argumentem "hej"

